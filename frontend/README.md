# SMC Smart Health Dashboard (frontend)

Install dependencies:

```bash
cd frontend
npm install
```

Run in development:

```bash
npm run dev
```

Set backend base URL with environment variable `REACT_APP_API_BASE` (defaults to `http://localhost:8000`).
